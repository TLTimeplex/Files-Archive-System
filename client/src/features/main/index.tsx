import React from "react";
import { Navigate } from "react-router";

export const Main = () => {

    //Navigate({to: '/login', replace: true});
    return (
        <>
        <h1>Hallo</h1>
        </>
    );
};

export default Main;